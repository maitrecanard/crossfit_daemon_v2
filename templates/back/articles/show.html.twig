{% extends 'back/back_base.html.twig' %}

{% block title %}Articles{% endblock %}

{% block body %}
        <h1 class="h3 mb-0 text-gray-800">{{ article.title }}</h1>
    </div>
       <a href="{{ path('app_back_articles_index') }}" class="btn btn-secondary btn-icon-split mb-4">
    <span class="icon text-white-50">
        <i class="fas fa-arrow-right"></i>
    </span>
    <span class="text">Retour à la liste</span>
</a>
    <div class="card shadow mb-4">
    <div class="card-body">
    <table class="table">
        <tbody>
            
            <tr>
                <th>Content</th>
                <td>{{ article.content|raw }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ article.activ ? 'En ligne' : 'hors ligne' }}</td>
            </tr>
        </tbody>
    </table>
                           </div>
                        </div>
               
 

    <a href="{{ path('app_back_articles_edit', {'id': article.id}) }}" class="btn btn-warning btn-icon-split mb-3">
                                        <span class="icon text-white-50">
                                           <i class="fas fa-arrow-right"></i>
                                        </span>
                                        <span class="text">Editer</span>
                                    </a>

 
{% endblock %}
