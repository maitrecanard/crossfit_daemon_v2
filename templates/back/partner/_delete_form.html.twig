<form method="post" action="{{ path('app_back_partner_delete', {'id': partner.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer le partenaire ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ partner.id) }}">
    <button class="btn btn-danger btn-icon-split">
                                        <span class="icon text-white-50">
                                            <i class="fas fa-trash"></i>
                                        </span>
                                        <span class="text">Supprimer</span>
                                    </button>
</form>
