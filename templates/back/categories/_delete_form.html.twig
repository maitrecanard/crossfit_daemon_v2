<form method="post" action="{{ path('app_back_categories_delete', {'id': category.id}) }}" onsubmit="return confirm('Etes-vous sur de voiloir supprimer cette catégories ? Attention cette action supprimera les produits liés');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ category.id) }}">
    <button class="btn btn-danger btn-icon-split">
                                        <span class="icon text-white-50">
                                            <i class="fas fa-trash"></i>
                                        </span>
                                        <span class="text">Supprimer</span>
                                    </button>
</form>
