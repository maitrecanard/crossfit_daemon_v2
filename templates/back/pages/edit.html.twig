{% extends 'back/back_base.html.twig' %}

{% block title %}Edit Pages{% endblock %}

{% block body %}
    <h1 class="h3 mb-0 text-gray-800">{{ page.title }}</h1>
</div>
    <a href="{{ path('app_back_pages_index') }}"class="btn btn-secondary btn-icon-split mb-4">
    <span class="icon text-white-50">
        <i class="fas fa-arrow-right"></i>
    </span>
    <span class="text">Retour à la liste</span>
</a>
<div class="card shadow mb-4">
    <div class="card-body">
    {{ include('back/pages/_form.html.twig', {'button_label': 'Enregistrer'}) }}
</div>
</div>


{% endblock %}
